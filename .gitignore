# secrets — bind-mounted at runtime from /data/local/dvlf/config.json, never in the image
config.json

# build artefacts
public/node_modules/
public/dist/
__pycache__/
*.pyc

# runtime output — the container logs to stdout now
api_server/*.log
nohup.out
