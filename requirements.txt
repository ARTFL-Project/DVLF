# Upgraded 2026-09-07, after the first weekly Trivy scan reported four HIGH CVEs against
# the 2022 starlette pin. Three of the four are multipart/form parsing issues and this app
# only accepts JSON bodies, so real exposure was lower than the count - but CVE-2026-48818
# (SSRF and NTLM credential theft via UNC paths) is not form-specific, and the fix for the
# strictest of them requires starlette 1.3.1, which in turn requires a modern fastapi.
#
# That makes pydantic v2 unavoidable. The application needs no change for it: datamodels.py
# declares three BaseModel classes of plain str fields with defaults, and no validators,
# Config classes or Field() constraints. Everything else the app returns is a dataclass,
# which FastAPI encodes the same way under either pydantic.
fastapi==0.141.1
starlette==1.6.0
pydantic==2.13.5

# Also flagged: CVE-2025-67221 against orjson 3.10.12, fixed in 3.11.6.
orjson==3.12.0

psycopg2-binary==2.9.12
python-Levenshtein==0.27.4
uvicorn==0.52.4
gunicorn==26.2.0
unidecode==1.4.0
pyhumps==3.8.0
requests==2.34.2
bleach==6.4.0
