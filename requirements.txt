# Tested on CPython 3.13, 2026-09-04/05. fastapi and starlette stay exactly as deployed
# in 2022 — moving off them means pydantic v2 and real code changes, which is a separate
# piece of work with its own verification (MIGRATION.md 8).
fastapi==0.78.0
starlette==0.19.1
pydantic>=1.10,<2

# The three bumps forced by a modern interpreter: the 2022 pins have no wheels for 3.13.
python-Levenshtein==0.27.1
orjson==3.10.12
psycopg2-binary==2.9.10

# Straightforward currency; no API change used by this app.
uvicorn==0.17.6
gunicorn==23.0.0
unidecode==1.3.8
pyhumps==3.8.0
requests==2.32.3
bleach==6.2.0
